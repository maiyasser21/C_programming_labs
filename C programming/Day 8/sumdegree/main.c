#include <stdio.h>
#include <stdlib.h>

void main()
{
    int*ptr=NULL;
    int size=0;
    int i=0;
    printf("enter array size : ");
    scanf("%d", &size);
    ptr=(int *) malloc(size);
    for (i=0; i<size; i++){
        printf("Array [%d] : %d ",i,  )
    }

}

